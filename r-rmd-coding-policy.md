# R/Rmd Coding Policy

이 문서는 Codex가 R code, R Markdown, R Notebook을 수정하거나 새로 작성할 때 따를 작업 기준이다. 목적은 코드가 과하게 추상화되지 않고, 노트북 흐름이 읽히며, 나중에 사용자가 직접 이어서 고치기 쉬운 상태를 유지하는 것이다.

이 문서는 R/Rmd 작업의 기본 선호다. 박사님의 현재 명시적 요청과 해당 작업에 적용되는 프로젝트 지침을 우선하고, 별도 지침이 없는 사항에 이 문서를 적용한다.

이 문서의 기본 선호와 다른 접근이 더 적합하면, 변경 이유와 장단점을 먼저 설명하고 제안한다. 이미 박사님이 요청하거나 승인한 범위는 반복해서 확인하지 않는다.

## 기본 원칙

- R 분석 코드는 `tidyverse`를 기본 도구로 사용할 수 있다.
- 다만 `tidyverse` 안에서도 experimental 기능, deprecated 기능, lifecycle이 불안정한 문법은 가급적 피한다.
- 이미 repo 안에서 안정적으로 쓰이는 패턴이 있으면 새 스타일을 만들지 말고 기존 패턴을 따른다.
- 복잡한 함수화보다 노트북의 분석 흐름이 보이는 청크 단위 구성을 우선한다.
- 함수는 같은 작업이 여러 번 반복되거나, 계산 의미를 이름으로 분명히 드러내는 경우에만 만든다.
- 연구 분석에 필요한 R package와 Python package는 자유롭게 제안하고, 이미 설치된 패키지는 작업 범위 안에서 사용할 수 있다.
- 새 설치나 기존 패키지 업데이트가 필요하면 대상과 필요성을 설명하고 확인받는다.
- 새 패키지 의존성은 명확한 이득이 있을 때만 추가한다. 결과 재현성이나 환경 복잡도를 크게 바꾸면 먼저 tradeoff를 설명한다.
- Python 패키지는 필요한 경우 `reticulate`의 `uv` 기반 환경을 우선 활용할 수 있다.
- Homebrew는 사용할 수 있지만, `brew install`처럼 시스템 전역 변경을 남기는 작업은 분석상 필요성이 명확할 때만 제안한다.
- 해킹 도구, 침투 테스트 도구, 보안 우회 목적 도구처럼 연구 분석 목적과 무관하거나 위험한 패키지와 프로그램은 설치하거나 사용하지 않는다.

## R 중심 연구 repo 구조

- 주 작업 방식은 R Notebook/R Markdown이며, 필요한 경우 `reticulate`를 통해 Python을 함께 사용할 수 있다.
- 설명 문장, 문서, 노트북 서술, 메모 등 사람이 읽는 내용은 가능하면 한글로 작성한다.
- 코드 객체명, 파일명, 패키지명, 함수명, 모델명처럼 영어가 자연스럽거나 기존 관례가 있는 부분은 영어를 유지한다.
- 파일과 폴더는 가능한 한 `자료 -> 빌드 -> 결과`의 시간 흐름을 따라 정리한다.
- 새 구조를 만들기 전에 기존 시간순 파일명과 폴더명 체계를 먼저 확인한다.
- 상위 폴더 이름의 첫 숫자는 소규모 연구 주제를 의미한다. 한 프로젝트에서 9개를 넘는 주제를 다루게 되면 `01`, `11`처럼 두 자리 번호 체계로 전환할 수 있다.
- 숫자 뒤의 `A`, `B`, `C` 등은 해당 주제 안에서 산출물을 시간순으로 나열하기 위한 구분이다.
- 폴더명은 가능하면 `주제번호 + 산출물순서 + 산출물종류/간단한내용 + 작업년도`를 포함한다.
- 파일명은 가능하면 날짜 접두어와 작업 내용을 포함해 시간순으로 이해할 수 있게 작성한다.
- `data-raw`는 원자료와 원자료 추출 스크립트를 주로 포함한다.
- `data-refined`는 재사용 가능한 정제 데이터 파일을 의미한다.
- 원자료, 비공개 자료, 외부 공개가 제한된 자료는 각 프로젝트의 사용권과 공개 범위를 따른다.
- 원자료를 역추산할 수 있는 표와 그림은 외부 발표물에 포함하지 않도록 주의한다.

## R Markdown / Notebook 구성

- Rmd는 날짜별 작업 기록과 분석 흐름이 보이는 노트북으로 작성한다.
- 큰 작업은 setup, 데이터 로드, 전처리, 확인, 모델링, 그림 저장, 요약처럼 의미 있는 청크로 나눈다.
- `html_notebook`에서 불필요한 전역 `knitr::opts_chunk$set()` 설정은 넣지 않는다.
- `html_notebook`에서 표 출력을 위해 `knitr::kable()`을 붙이지 않는다. tibble/data.frame 자체를 출력한다.
- Rmd에서 필요 없는 코드펜스, 예를 들어 단순 설명용 triple-backtick `text` 블록은 쓰지 않는다.
- 사람이 읽는 설명, 해석, 메모는 가능하면 한글로 쓴다.
- 함수명, 객체명, 파일명, 모델명, 패키지명처럼 영어가 자연스러운 부분은 영어를 유지한다.

## RStudio 중심 EDA 협업

RStudio/R Notebook에서 박사님이 직접 청크를 실행하고, Codex는 코드 작성, 해석, 정리, 재현성 보강을 돕는 협업 방식을 기본으로 한다. EDA 단계에서는 빠른 반복과 도메인 판단을 우선하며, 전체 render나 긴 batch 실행을 기본 요구사항으로 삼지 않는다.

- Codex는 RStudio 화면이나 현재 interactive R session을 직접 본다고 가정하지 않는다.
- 박사님이 RStudio에서 청크를 실행하고, 필요한 출력, 에러, 경고, 그래프 관찰을 공유하는 흐름을 우선한다.
- Codex는 공유된 출력과 관찰을 바탕으로 다음 분석 방향, 짧은 chunk, 코드 수정, 해석 정리를 제안한다.
- EDA 단계에서 Codex가 제안하는 chunk는 기본적으로 10~30줄 안팎으로 짧게 유지한다.
- chunk에는 가능하면 `glimpse()`, `summary()`, `count()`, `skimr::skim()`, `head()`, `warnings()` 같은 텍스트 확인 출력을 포함한다.
- 모델 확인에는 `summary(model)`, `broom::tidy(model)`, `broom::glance(model)` 같은 요약 출력을 우선한다.
- 그래프를 그릴 때도 가능하면 숫자 요약을 함께 확인한다.
- 그래프를 논의할 때는 축 범위, 이상치, 그룹 간 차이, facet별 패턴, 예상과 다른 점 같은 시각적 관찰을 한두 문장으로 공유받고, 필요한 경우 그 관찰을 확인할 추가 요약 코드를 제안한다.
- 에러가 발생하면 우선 에러 메시지 그대로를 바탕으로 짧은 수정 chunk를 제안한다.
- 필요할 때만 `warnings()`, `traceback()`, `rlang::last_trace()`, `sessionInfo()` 같은 추가 확인 코드를 요청하거나 제안한다.
- 에러 진단용 코드는 짧게 유지하고, 본 분석 코드를 불필요하게 복잡하게 만들지 않는다.
- 긴 계산을 반복 실행할 필요가 생기면 별도 스크립트나 `.rds` 캐시 사용을 제안한다. 분리는 요청 범위와 기존 프로젝트 구조에 맞춰 수행한다.
- 재현성 점검, 최종 정리, 캐시 생성, 결과물 검증이 필요한 단계에서는 `rmarkdown::render()`, `quarto render`, `Rscript` 실행을 제안하거나 수행할 수 있다.
- 다음 중 하나 이상에 해당하면 EDA 코드를 정리 가능한 형태로 바꾸는 것을 제안할 수 있다: 사용할 변수와 필터 조건이 확정됨, 결측/이상치/단위 변환/그룹 정의 방침이 정해짐, 새 세션에서 재현할 필요가 생김, 표나 그래프가 보고서 후보가 됨, 계산 시간이 길어져 캐시나 별도 스크립트가 필요해짐.
- 최종 정리 단계에서는 interactive session에만 존재하는 객체에 의존하지 않도록 코드를 재현 가능한 형태로 정리한다.
- EDA에서는 박사님이 RStudio에서 청크를 실행하고 결과를 공유하는 흐름을 기본으로 한다. 박사님이 실행이나 검증을 요청한 경우에는 Codex가 사용 가능한 환경에서 수행하되, 별도 실행 환경을 박사님의 현재 R session과 동일하다고 가정하지 않는다.
- Codex는 요청 범위 안에서 코드 제안, 결과 해석, 오류 수정, 리팩터링, 재현성 보강, 문서화, 파일 정리를 담당한다.

## 코드 스타일

- pipe는 base pipe `|>`를 우선 사용한다.
- 긴 pipe chain이나 여러 줄 할당문은 왼쪽 변수명과 `<-`만 먼저 쓰고 줄을 바꾼 뒤 오른쪽 표현식을 시작한다.

```r
curve_property_check <-
  curves_joined |>
  arrange(specimen_number, strain_in_in) |>
  summarize(...)
```

- 한 줄에 너무 많은 작업을 넣지 말고, 중간 객체를 적절히 만들어 검토 가능하게 둔다.
- 중간 객체 이름은 `*_raw`, `*_joined`, `*_plot`, `*_summary`, `*_check`처럼 역할이 드러나게 붙인다.
- 단위 변환 함수처럼 의미가 분명하고 자주 쓰는 함수는 짧고 읽기 쉬운 이름을 쓴다. 예: `ksi_to_MPa()`, `F_to_C()`.
- 문자열 파싱, factor level 조정, join key 정리는 별도 청크나 별도 mutate 블록으로 분리해 실수를 확인하기 쉽게 한다.
- `rowwise()`는 필요한 경우에만 제한적으로 사용한다. 벡터화, `pmap()`, list-column이 더 명확하면 그쪽을 우선 검토한다.

## tidyverse 사용 기준

- `dplyr`, `ggplot2`, `readr`, `stringr`, `forcats`, `tidyr`, `purrr` 등 널리 쓰이고 안정적인 tidyverse 기능을 우선한다.
- deprecated 함수는 새 코드에 쓰지 않는다. 예: `mutate_at()`, `summarise_at()`, `gather()`, `spread()`, `aes_string()`.
- lifecycle이 experimental로 표시된 기능은 꼭 필요한 경우가 아니면 피한다.
- `across()`는 안정적인 column-wise 작업에 사용한다.
- `.by`는 간단한 그룹 요약에서 사용할 수 있지만, 긴 분석 흐름이나 여러 단계 그룹 조작에서는 `group_by()` / `ungroup()`이 더 읽기 쉬운지 검토한다.
- `reframe()`, `pick()`처럼 비교적 새 문법은 기존 코드와 맞고 의도가 명확할 때만 사용한다.
- factor 순서가 plot facet이나 legend와 연결되는 경우 `fct_relevel()`, `fct_recode()` 등을 명시적으로 사용한다.

## Plotting Policy

- 최종 결과 그림과 중간 확인 그림을 구분한다.
- 중간 확인용 그림은 과도한 색상, theme, annotation을 넣지 않고 확인 목적에 집중한다.
- 색상은 그룹 구분이 실제 해석에 필요할 때만 사용한다.
- 여러 시편이나 조건을 비교할 때 facet 구조를 먼저 검토한다.
- `facet_wrap()`은 순서가 중요한 small multiples에 사용하고, 행/열 의미가 분명한 경우에만 `facet_grid()`를 쓴다.
- 같은 시편의 저온/고온 조건처럼 행/열 의미가 중요한 경우 factor level과 facet 배치가 데이터 의미와 맞는지 확인한다.
- point, line, facet에 사용하는 데이터가 다르면 두 데이터 모두 같은 facet variable과 factor level을 갖도록 맞춘다.
- legend는 꼭 필요한 mapping만 남긴다. 점 모양, 선 종류, 색의 의미가 겹치면 하나를 줄이거나 설명을 caption/note로 옮긴다.
- caption은 그림이 실제로 보여주는 내용과 맞아야 한다. 이전 그림에서 복사한 caption을 그대로 두지 않는다.

## 데이터 처리와 재현성

- 입력 파일 경로, 결과 폴더, 저장 파일 이름은 노트북 상단에서 확인 가능하게 둔다.
- 경로는 프로젝트의 기존 방식을 따른다. 상대경로를 사용할 때는 청크 실행과 문서 렌더링의 기준 디렉터리를 확인하고, `..`가 항상 저장소 루트라고 가정하지 않는다.
- 개인 환경에 종속된 절대경로를 코드 여러 곳에 반복해서 넣지 않는다. 필요한 경로는 설정 위치에 모으고, 임시 경로는 필요한 범위에서만 사용한다.
- 원자료에서 가져온 값과 digitized/계산값이 섞이는 경우 이름에 `table`, `digitized`, `curve`, `model` 등을 넣어 출처를 구분한다.
- 단위는 컬럼명에 명시한다. 예: `stress_MPa`, `stress_ksi`, `test_temp_C`, `test_temp_F`.
- 저장된 결과물은 노트북 목적과 폴더 이름이 맞도록 유지한다.

## 수정 범위

- 사용자가 특정 노트북이나 청크를 지정하면 그 범위 안에서 우선 해결한다.
- 전체 코드 리팩토링, 폴더 재구성, 공통 utility 정리는 명시적으로 요청받았을 때만 한다.
- 관련 없는 파일의 포맷팅, 이름 변경, helper 이동은 하지 않는다.
- 기존 미커밋 변경을 보존한다. 요청 수행에 필요한 부분만 수정하며, 기존 변경과 충돌해 의도를 판단하기 어려우면 박사님께 확인한다.

## 검토 기준

- 코드 변경 후에는 최소한 문법적으로 눈에 띄는 오류, 잘못된 객체명, 잘못된 caption, facet/point 데이터 불일치를 확인한다.
- 실행 검증이 필요한 경우 가능한 가장 작은 청크나 관련 노트북 단위로 확인한다.
- 검증하지 못한 부분은 최종 보고에 명확히 남긴다.
