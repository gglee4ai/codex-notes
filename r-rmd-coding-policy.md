# R/Rmd Coding Policy

박사님이 직접 읽고 이어서 수정하기 쉬운 R/Rmd 코드를 작성한다. 이 문서는 기본 선호이며, 박사님의 현재 요청과 프로젝트별 지침을 우선한다. 기본 선호와 다른 접근이 더 적합하면 이유와 장단점을 먼저 제안하되, 이미 승인된 범위는 반복 확인하지 않는다.

## 협업 방식

- RStudio에서 박사님이 청크를 실행하고 결과를 공유하는 EDA 흐름을 기본으로 한다. Codex는 코드 작성, 해석, 오류 수정과 정리를 돕는다.
- 현재 RStudio 화면이나 R session을 직접 본다고 가정하지 않는다. 실행·검증을 요청받으면 사용 가능한 환경에서 수행하고, 박사님의 세션과 구분한다.
- 설명과 메모는 가능하면 한글로 쓴다. 객체명·파일명·함수명 등은 기존 영어 관례를 따른다.
- EDA에서는 짧은 청크와 필요한 요약 출력을 우선하며, 전체 render나 긴 일괄 실행을 기본 요구사항으로 삼지 않는다.
- 오류는 공유된 메시지부터 확인하고, 진단 코드는 짧게 유지한다.

## 코드와 노트북

- 기존 코드 스타일을 존중한다. R 분석에는 `tidyverse`를 사용할 수 있으며, pipe는 base pipe `|>`를 우선 사용한다.
- 프로젝트의 패키지 버전에서 지원되는 안정적인 기능을 우선한다. 신규 코드에는 권장 문법을 쓰되, 기존 코드를 문법 교체만을 위해 수정하지 않는다.
- 분석 흐름이 보이도록 로드, 전처리, 확인, 모델링, 결과 정리를 의미 있는 청크로 나눈다. EDA 청크는 보통 10~30줄 안팎을 목표로 한다.
- 불필요한 함수화와 추상화를 피한다. 반복 계산이나 의미가 분명한 작업만 함수로 만들고, 중간 객체는 역할이 드러나게 이름 붙인다.
- 문자열 파싱, factor 순서, join 키 정리는 구분해서 확인하기 쉽게 작성한다.
- 긴 할당문은 객체명과 `<-` 다음 줄에서 시작한다.

```r
property_summary <-
  property_data |>
  group_by(condition) |>
  summarise(n = n(), .groups = "drop")
```

- `html_notebook`에서는 `knitr::kable()` 대신 tibble/data.frame 자체를 출력한다. 불필요한 전역 청크 옵션과 설명용 코드펜스는 넣지 않는다.

## 패키지와 환경

- 필요한 R/Python 패키지는 제안하고, 이미 설치된 패키지는 작업 범위에서 사용할 수 있다. 새 설치·업데이트나 Homebrew 설치는 대상과 필요성을 설명하고 확인받는다.
- 새 의존성은 이득이 분명할 때만 추가한다. Python 연동은 필요하면 `reticulate`와 `uv` 기반 환경을 우선 고려한다.

## 폴더와 파일 구조

- 새 파일이나 폴더를 만들기 전에 기존 구조와 명명 규칙을 확인한다. 자료 → 빌드 → 결과의 흐름과 시간순 작업 기록이 드러나게 정리한다.
- 상위 폴더의 숫자는 소규모 연구 주제를, 뒤의 `A`, `B`, `C` 등은 해당 주제 안에서 산출물의 시간순을 나타낸다.
- 폴더명은 가능하면 `주제번호 + 산출물순서 + 산출물종류/간단한내용 + 작업년도`로 구성한다. 작업 기록과 산출물의 파일명은 가능하면 날짜 접두어와 작업 내용을 포함한다.
- 연구 주제가 9개를 넘으면 두 자리 주제번호 체계를 제안할 수 있지만, 기존 폴더의 일괄 이름 변경은 요청받았을 때만 한다.
- `data-raw`는 원자료와 추출 스크립트, `data-refined`는 재사용 가능한 정제 데이터를 의미한다. 폴더 생성은 필요에 따른다.
- 입력 경로와 저장 위치는 확인하기 쉽게 모으고, 실행 기준 디렉터리를 확인한다.

## 데이터 처리

- 원자료를 보존하고 정제·계산 결과와 구분한다. 원자료 값과 digitized·계산값의 출처를 이름이나 설명에 명시한다.
- 자료와 표·그림의 공개 범위는 프로젝트의 제한을 따른다. 공개가 제한된 원자료를 역추산할 수 있는 표·그림도 외부 공개 대상에 포함하지 않는다.

## 그림과 검증

- 중간 그림은 확인 목적에 맞게 단순하게 만든다. 조건 비교에는 facet을 고려하고, 축·단위·패널 순서·범례·caption이 데이터와 맞는지 확인한다.
- 서로 다른 데이터를 쓰는 레이어는 의도한 패널에 표시되는지 확인한다. 공통 기준선이나 주석의 반복 표시는 허용한다.
- 검증은 변경 범위에 맞게 수행하고, 실행하지 않은 부분은 명확히 알린다.

## 수정 범위

- 요청과 직접 관련된 최소 범위만 수정한다. 전체 리팩터링이나 폴더 재구성은 별도로 요청받았을 때만 한다.
